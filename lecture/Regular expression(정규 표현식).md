# Regular expression(정규 표현식)
정규 표현식이라는 개념을 처음 들어본 건 채팅 내용 중 욕설이나 직거래 제안이 있는지 감지하기 위한 기능을 구현할 때였다. 당시 개발자가 '직거래라고 취급할만한 정규 표현식을 알려주세요'라고 말했다. 개발자의 요청을 인간의 언어로 변환하자면 '직거래라고 의심할만한 메시지의 패턴을 알려달라'는 거였다. 사람은 글을 읽으면 다르게 표현된 것도 같은 의미로 받아들이지만, 컴퓨터는-더 정확히는 AI 가 적용되지 않은 알고리즘은-룰을 명확하게 정의해주어야 한다. </br>
정규 표현식을 활용한 알고리즘은 쉽게 찾아볼 수 있다. 인스타그램 계정을 생성할 때, 아이디에 `_`나 `.`를 포함할 수 있지만 맨 앞글자는 알파벳 혹은 숫자여야한다. 또한 아이디에는 대문자 알파벳은 포함할 수 없다. 혹은 어떤 웹서비스에 이메일로 가입해야할 때, `hello12345@gmail.com`의 형태로 기입해야한다. </br>
파이썬으로 어떤 단어, 문장 등의 문자열이 내가 원하는 패턴에 부합하는지 확인해보자. 
## 1. 정규표현식이란? 
### 메타 문자
### [] 
### .
### * 
### +
### {}
### ? 
### ^ 
### $
## 2. re 모듈과 검색 
```python
import re
```
우선 `re` 모듈을 불러오는 것으로 시작한다. 

### 2.1. re.compile()
```python
pattern = re.compile('정규표현식')
```
- `compile` 메소드는 임의의 정규표현식을 인자로 받고, 패턴 객체를 반환한다. 
- 반환된 패턴 객체에 검색 메소드를 활용하여, 임의의 문자열이 정규표현식에 부합하는지를 확인한다. 
### 2.2. 문자열 검색 메소드: match, search, findall, finditer
#### match()
##### match 객체 메서드
1. group 
2. start
3. end
4. span
#### search()
#### findall()
#### fintiter()
#### 축약 형태 사용
축약 전 
```python
# 축약 전 
p = re.compile(정규표현식)
m = p.match(문자열)

# 축약 후
m = p.match(정규표현식, 문자열) 
```
- compile과 match 메소드를 동시에 수행할 수 있다. 
### 2.3. 

## 2. 
```python
import re 
``` 
`re` 모듈을 `import`하며 시작해보자. `re`는 regular expression의 약자이다. 
```python
patternObject = re.compile("regular expression")
```
`re` 모듈의 `compile` 함수에 임의의 정규 표현식을 인자로 전달하면 패턴 객체를 반환한다. 그리고 그 패턴 객체 아래 4가지 메소드를 사용할 수 있다.
### 메소드 
|메소드|목적|반환값|
|match()|정규식과 매치되는지 문자열의 처음부터 조사한다|매치되면 Match 객체, 매치되지 않으면 None 반환|
|search()|정규식과 매치되는지 문자열 전체를 조사한다|매치되면 Match 객체, 매치되지 않으면 None 반환|
|findall()|정규식과 매치되는지 문자열 전체를 조사한다|리스트로 반환|
|finditer()|정규식과 매치되는지 문자열 전체를 조사한다|반복 가능한 객체(iter)를 반환|